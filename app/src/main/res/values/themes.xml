<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Companion" parent="Theme.MaterialComponents.NoActionBar">

        <!-- Status bar color. -->
        <item name="android:statusBarColor"
            tools:targetApi="l">@color/forestGreenStatus</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Companion.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Companion.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Companion.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>